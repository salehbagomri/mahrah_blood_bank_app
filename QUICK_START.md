# دليل البدء السريع - بنك دم محافظة المهرة

## 🚀 البدء السريع في 5 دقائق

### 1. تثبيت Flutter (إذا لم يكن مثبتاً)

#### Windows
```bash
# تحميل Flutter SDK
# من: https://docs.flutter.dev/get-started/install/windows

# إضافة Flutter للـ PATH
# تحقق من التثبيت:
flutter doctor
```

#### macOS/Linux
```bash
# تحميل Flutter SDK
# من: https://docs.flutter.dev/get-started/install

# تحقق من التثبيت:
flutter doctor
```

### 2. استنساخ المشروع

```bash
git clone https://github.com/yourusername/mahrah_blood_bank_app.git
cd mahrah_blood_bank_app
```

### 3. تثبيت التبعيات

```bash
flutter pub get
```

### 4. إعداد Supabase (خطوات سريعة)

#### أ) إنشاء مشروع Supabase

1. اذهب إلى [https://app.supabase.com/](https://app.supabase.com/)
2. سجّل دخول أو أنشئ حساب جديد (مجاني)
3. اضغط "New Project"
4. املأ البيانات:
   - **Name**: mahrah-blood-bank
   - **Password**: اختر كلمة مرور قوية
   - **Region**: اختر أقرب منطقة
5. اضغط "Create new project"

#### ب) إعداد قاعدة البيانات

1. بعد إنشاء المشروع، انتقل إلى **SQL Editor**
2. اضغط **New Query**
3. انسخ محتوى ملف `supabase_schema.sql` والصقه
4. اضغط **Run** (أو Ctrl+Enter)
5. انتظر حتى يكتمل التنفيذ (يستغرق ثوانٍ)

#### ج) الحصول على المفاتيح

1. اذهب إلى **Settings** > **API**
2. انسخ:
   - **Project URL**
   - **anon public key**

#### د) تكوين التطبيق

1. افتح ملف `lib/config/supabase_config.dart`
2. استبدل القيم:

```dart
static const String supabaseUrl = 'YOUR_PROJECT_URL_HERE';
static const String supabaseAnonKey = 'YOUR_ANON_KEY_HERE';
```

### 5. تشغيل التطبيق

```bash
# للأندرويد/iOS
flutter run

# للويب
flutter run -d chrome

# لنظام معين
flutter run -d <device_id>

# لرؤية الأجهزة المتاحة:
flutter devices
```

---

## 🎯 اختبار التطبيق

### أ) كمستخدم عادي

1. ✅ افتح التطبيق
2. ✅ اضغط "البحث عن متبرعين"
3. ✅ اختر فصيلة دم ومديرية
4. ✅ اضغط "بحث"
5. ✅ جرّب "إضافة متبرع"

### ب) إنشاء حساب مستشفى (للتجربة)

1. اذهب إلى Supabase Dashboard
2. **Authentication** > **Users** > **Add user**
3. أنشئ مستخدم:
   - Email: `hospital@test.com`
   - Password: `123456`
   - ✅ Auto Confirm User
4. انسخ الـ **User UID**
5. اذهب إلى **SQL Editor** ونفّذ:

```sql
-- استبدل USER_UID بالمعرف الذي نسخته
INSERT INTO hospitals (id, name, email, district, is_active)
VALUES (
    'USER_UID_HERE',
    'مستشفى الغيضة',
    'hospital@test.com',
    'الغيضة',
    TRUE
);
```

6. الآن يمكنك تسجيل الدخول من التطبيق كمستشفى

### ج) إنشاء حساب أدمن

```sql
-- بعد إنشاء المستخدم في Auth، نفّذ:
INSERT INTO admins (id, name, email, is_active)
VALUES (
    'USER_UID_HERE',
    'المدير العام',
    'admin@mahrah.com',
    TRUE
);
```

---

## 📱 اختبار الميزات الأساسية

### ✅ قائمة الفحص

- [ ] البحث عن متبرعين
- [ ] إضافة متبرع جديد
- [ ] الاتصال بمتبرع (Phone/WhatsApp)
- [ ] قراءة صفحة التوعية
- [ ] إرسال بلاغ
- [ ] عرض الإحصائيات
- [ ] تسجيل دخول مستشفى
- [ ] تسجيل دخول أدمن

---

## 🔧 حل المشاكل الشائعة

### مشكلة: "Supabase not configured"

**الحل**: تأكد من تحديث `lib/config/supabase_config.dart` بالمفاتيح الصحيحة

### مشكلة: خطأ في الاتصال بقاعدة البيانات

**الحل**: 
1. تحقق من اتصالك بالإنترنت
2. تحقق من أن مشروع Supabase يعمل
3. تحقق من صحة المفاتيح

### مشكلة: "Permission denied" عند البحث

**الحل**: تأكد من تنفيذ `supabase_schema.sql` كاملاً (خاصةً RLS Policies)

### مشكلة: لا تظهر المتبرعين في البحث

**الحل**: أضف بيانات تجريبية:

```sql
INSERT INTO donors (name, phone_number, blood_type, district, age, gender)
VALUES 
    ('أحمد محمد', '777123456', 'A+', 'الغيضة', 25, 'male'),
    ('فاطمة علي', '777234567', 'B+', 'سيحوت', 30, 'female'),
    ('خالد سعيد', '777345678', 'O+', 'حصوين', 28, 'male');
```

---

## 📚 الخطوات التالية

1. 📖 اقرأ [README.md](README.md) للتفاصيل الكاملة
2. 🔧 راجع [SUPABASE_SETUP.md](SUPABASE_SETUP.md) لإعدادات متقدمة
3. 🤝 اقرأ [CONTRIBUTING.md](CONTRIBUTING.md) للمساهمة

---

## 💡 نصائح

- استخدم **Hot Reload** في Flutter (اضغط `r` في Terminal)
- استخدم **Hot Restart** (اضغط `R` في Terminal)
- افحص logs في Supabase Dashboard > Logs
- راجع الإحصائيات في Supabase > Database > Statistics

---

## 🆘 الدعم

إذا واجهت مشكلة:
1. راجع [Issues](https://github.com/yourusername/mahrah_blood_bank_app/issues)
2. افتح Issue جديد مع:
   - وصف المشكلة
   - خطوات إعادة الإنتاج
   - لقطة شاشة (إن أمكن)
   - نسخة Flutter: `flutter --version`

---

**🎉 تهانينا! أنت الآن جاهز للبدء**

كل ما تحتاجه هو 5 دقائق والتطبيق سيعمل! 🚀

