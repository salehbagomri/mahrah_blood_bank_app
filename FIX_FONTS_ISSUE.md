# حل مشكلة عدم ظهور الخط

## 🔧 الحل السريع

### الخطوة 1: إيقاف التطبيق تماماً
اضغط على زر Stop (المربع الأحمر) في VS Code أو Android Studio

### الخطوة 2: نفّذ الأوامر التالية بالترتيب

```bash
# 1. تنظيف الكاش
flutter clean

# 2. استعادة الحزم
flutter pub get

# 3. إعادة البناء والتشغيل
flutter run
```

**مهم جداً**: استخدم **Restart (R)** وليس Hot Reload عند إعادة التشغيل!

---

## ⚡ الحل الأسرع

إذا كان التطبيق يعمل الآن:
1. اضغط **Shift + R** (أو اكتب `R` في Terminal)
2. هذا يعمل **Hot Restart** بدلاً من Hot Reload
3. الخط سيظهر بعد إعادة التشغيل الكامل

---

## 🔍 سبب المشكلة

**Hot Reload** لا يُحمّل الخطوط الجديدة، يجب استخدام:
- ✅ **Hot Restart** (Shift + R أو زر ⟲)
- ✅ أو **Full Restart** (إيقاف وتشغيل من جديد)

---

## ✅ كيف تتأكد أن الخط يعمل؟

### بعد إعادة التشغيل، ابحث عن:
1. **العناوين** يجب أن تكون واضحة ومقروءة بخط IBM Plex Sans Arabic
2. **الأزرار** النصوص يجب أن تكون بنفس الخط
3. **النصوص العربية** يجب أن تظهر بشكل احترافي

### مقارنة:
- ❌ **الخط الافتراضي**: يبدو عادياً وبسيطاً
- ✅ **IBM Plex Sans Arabic**: يبدو احترافياً ومقروءاً أكثر

---

## 🌐 في المرة الأولى فقط

الخط يحتاج اتصال بالإنترنت في **المرة الأولى فقط** لتحميله من Google Fonts.

**بعد ذلك**:
- ✅ يُخزّن محلياً (Cache)
- ✅ يعمل بدون إنترنت

---

## 🐛 إذا لم يعمل الحل؟

### جرب هذا:

```bash
# 1. حذف مجلد build يدوياً
rm -rf build
# (أو احذفه من File Explorer)

# 2. تنظيف كامل
flutter clean

# 3. استعادة الحزم
flutter pub get

# 4. إعادة البناء
flutter run
```

### أو استخدم Hot Restart المباشر:
1. في Terminal حيث يعمل التطبيق
2. اضغط **R** (حرف R كبير)
3. انتظر إعادة التشغيل الكامل

---

## 📱 للتأكد من التحميل

في أول مرة تشغل التطبيق:
1. تأكد من وجود اتصال إنترنت
2. انتظر قليلاً (5-10 ثواني) عند أول شاشة
3. الخط سيُحمّل تلقائياً
4. في المرات القادمة يعمل فوراً

---

## 🎯 الأوامر السريعة

```bash
# تنظيف + تشغيل
flutter clean && flutter pub get && flutter run

# أو فقط
flutter run --no-hot
```

---

## ✨ النتيجة المتوقعة

بعد تطبيق الحل:
- ✅ جميع النصوص العربية بخط IBM Plex Sans Arabic
- ✅ قراءة أفضل وأوضح
- ✅ مظهر احترافي

---

**ملاحظة**: الخطوط من Google Fonts تحتاج **Hot Restart** وليس Hot Reload لتظهر للمرة الأولى!

