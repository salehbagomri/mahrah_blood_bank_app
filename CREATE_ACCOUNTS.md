# دليل إنشاء حسابات الأدمن والمستشفيات

## 🔐 كيفية الوصول إلى لوحات التحكم

### للوصول إلى واجهة الأدمن أو المستشفى:

1. **من الصفحة الرئيسية للتطبيق**
   - اضغط على أيقونة 🔑 (Login) في أعلى يسار الشاشة
   - أو ستجد زر "تسجيل الدخول" في شريط التطبيق

2. **أدخل بيانات الدخول**
   - البريد الإلكتروني
   - كلمة المرور

3. **سيتم توجيهك تلقائياً**
   - إلى لوحة الأدمن (إذا كنت أدمن)
   - إلى لوحة المستشفى (إذا كنت مستشفى)

---

## 👨‍💼 إنشاء حساب أدمن (Admin)

### الخطوة 1: إنشاء مستخدم في Supabase Auth

1. افتح [Supabase Dashboard](https://app.supabase.com/)
2. اختر مشروعك: **mahrah-blood-bank**
3. اذهب إلى **Authentication** → **Users**
4. اضغط على **Add user** → **Create new user**
5. املأ البيانات:
   ```
   Email: admin@mahrah-blood-bank.com
   Password: [اختر كلمة مرور قوية]
   ✅ Auto Confirm User (فعّل هذا الخيار)
   ```
6. اضغط **Create user**
7. **انسخ User UID** من قائمة المستخدمين (معرف طويل مثل: `abc123...`)

### الخطوة 2: إضافة المستخدم إلى جدول admins

1. اذهب إلى **SQL Editor** → **New Query**
2. الصق الكود التالي واستبدل `YOUR_USER_UID` بالمعرف الذي نسخته:

```sql
-- إنشاء حساب أدمن
INSERT INTO admins (id, name, email, is_active)
VALUES (
    'YOUR_USER_UID',  -- ⚠️ استبدل هذا بالـ UID الفعلي
    'المدير العام',
    'admin@mahrah-blood-bank.com',
    TRUE
);
```

3. اضغط **Run** أو `Ctrl+Enter`

### الخطوة 3: تسجيل الدخول

الآن يمكنك تسجيل الدخول:
- **Email**: `admin@mahrah-blood-bank.com`
- **Password**: [كلمة المرور التي اخترتها]

---

## 🏥 إنشاء حساب مستشفى (Hospital)

### الخطوة 1: إنشاء مستخدم في Supabase Auth

1. افتح [Supabase Dashboard](https://app.supabase.com/)
2. اختر مشروعك: **mahrah-blood-bank**
3. اذهب إلى **Authentication** → **Users**
4. اضغط على **Add user** → **Create new user**
5. املأ البيانات:
   ```
   Email: hospital.ghaidah@mahrah.health.gov.ye
   Password: [كلمة مرور قوية]
   ✅ Auto Confirm User
   ```
6. اضغط **Create user**
7. **انسخ User UID**

### الخطوة 2: إضافة المستشفى إلى جدول hospitals

```sql
-- إنشاء حساب مستشفى الغيضة
INSERT INTO hospitals (id, name, email, district, phone_number, address, is_active)
VALUES (
    'YOUR_USER_UID',  -- ⚠️ استبدل هذا بالـ UID الفعلي
    'مستشفى الغيضة العام',
    'hospital.ghaidah@mahrah.health.gov.ye',
    'الغيضة',
    '777000000',
    'مدينة الغيضة - محافظة المهرة',
    TRUE
);
```

### أمثلة لمستشفيات أخرى:

#### مستشفى سيحوت
```sql
INSERT INTO hospitals (id, name, email, district, is_active)
VALUES (
    'USER_UID_HERE',
    'مستشفى سيحوت',
    'hospital.sayhut@mahrah.health.gov.ye',
    'سيحوت',
    TRUE
);
```

#### مستشفى حصوين
```sql
INSERT INTO hospitals (id, name, email, district, is_active)
VALUES (
    'USER_UID_HERE',
    'مستشفى حصوين',
    'hospital.haswin@mahrah.health.gov.ye',
    'حصوين',
    TRUE
);
```

---

## 🧪 حسابات تجريبية للاختبار

### Admin Test Account
```
Email: admin@test.com
Password: test123456
```

**⚠️ لا تستخدم هذا في Production!**

إنشاء الحساب:
```sql
-- 1. أنشئ المستخدم في Auth أولاً
-- 2. ثم نفّذ:
INSERT INTO admins (id, name, email, is_active)
VALUES (
    'USER_UID_FROM_AUTH',
    'Admin Test',
    'admin@test.com',
    TRUE
);
```

### Hospital Test Account
```
Email: hospital@test.com
Password: test123456
```

```sql
INSERT INTO hospitals (id, name, email, district, is_active)
VALUES (
    'USER_UID_FROM_AUTH',
    'مستشفى تجريبي',
    'hospital@test.com',
    'الغيضة',
    TRUE
);
```

---

## 🔍 التحقق من نجاح العملية

### للتحقق من الأدمن:
```sql
SELECT * FROM admins;
```

### للتحقق من المستشفيات:
```sql
SELECT * FROM hospitals;
```

### للتحقق من المستخدمين في Auth:
- اذهب إلى **Authentication** → **Users**
- يجب أن ترى المستخدمين الذين أنشأتهم

---

## 🔒 نصائح الأمان

1. **استخدم كلمات مرور قوية**
   - 12 حرف على الأقل
   - مزيج من أحرف كبيرة وصغيرة وأرقام ورموز

2. **لا تشارك بيانات الدخول**
   - كل مستشفى يجب أن يكون لها حساب خاص

3. **غيّر كلمات المرور دورياً**

4. **احذف الحسابات التجريبية**
   - قبل النشر في Production

---

## ❌ حذف حساب (إذا لزم الأمر)

### حذف أدمن:
```sql
-- احذف من جدول admins أولاً
DELETE FROM admins WHERE email = 'admin@example.com';

-- ثم احذف من Auth (من Dashboard)
-- Authentication > Users > Delete user
```

### حذف مستشفى:
```sql
-- احذف من جدول hospitals أولاً
DELETE FROM hospitals WHERE email = 'hospital@example.com';

-- ثم احذف من Auth
```

---

## 🆘 استكشاف الأخطاء

### مشكلة: لا يمكن تسجيل الدخول

**الحلول:**
1. تأكد من أن البريد وكلمة المرور صحيحة
2. تأكد من أن المستخدم موجود في Auth
3. تأكد من أن المستخدم موجود في جدول admins أو hospitals
4. تأكد من أن `is_active = TRUE`

### مشكلة: "لا يمكن تحديد نوع المستخدم"

**الحل:**
- تأكد من أن الـ UID في جدول admins/hospitals يطابق الـ UID في Auth

### مشكلة: "Permission denied"

**الحل:**
- تأكد من تنفيذ `supabase_schema.sql` كاملاً
- تحقق من RLS Policies في Supabase

---

## 📊 عرض جميع الحسابات

```sql
-- عرض جميع الأدمن
SELECT 
    a.id,
    a.name,
    a.email,
    a.is_active,
    a.created_at
FROM admins a
ORDER BY a.created_at DESC;

-- عرض جميع المستشفيات
SELECT 
    h.id,
    h.name,
    h.email,
    h.district,
    h.is_active,
    h.created_at
FROM hospitals h
ORDER BY h.created_at DESC;
```

---

## ✅ الخلاصة

### للوصول إلى لوحة التحكم:

1. **أنشئ مستخدم في Supabase Auth**
2. **أضف المستخدم إلى جدول admins أو hospitals**
3. **افتح التطبيق واضغط على أيقونة Login**
4. **سجّل دخولك**
5. **استمتع بصلاحياتك!** 🎉

---

**💡 نصيحة**: احفظ بيانات الدخول في مكان آمن!

**🩸 التبرع بالدم ينقذ الأرواح**

