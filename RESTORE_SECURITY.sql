-- ============================================
-- استعادة الأمان بعد الاختبار
-- ============================================

-- استخدم هذا بعد TEMPORARY_BYPASS.sql
-- لإعادة تطبيق السياسات الآمنة

-- ============================================

-- حذف السياسة المؤقتة
DROP POLICY IF EXISTS "hospitals_insert_policy_temp" ON public.hospitals;

-- إعادة إنشاء السياسة الآمنة
CREATE POLICY "hospitals_insert_policy"
ON public.hospitals
FOR INSERT
TO authenticated
WITH CHECK (
  public.is_admin() = true
);

-- ============================================

SELECT '✅ تم استعادة الأمان!' as "الحالة";
SELECT '🔒 الآن فقط الأدمن يمكنهم إضافة مستشفيات' as "النتيجة";

-- ============================================

