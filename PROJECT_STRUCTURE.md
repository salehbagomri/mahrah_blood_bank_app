# هيكل المشروع التفصيلي

## 📂 نظرة شاملة على بنية المشروع

```
mahrah_blood_bank_app/
│
├── 📁 android/                      # ملفات Android
├── 📁 assets/                       # الأصول (صور، أيقونات)
│   ├── images/                      # الصور
│   └── icons/                       # الأيقونات
│
├── 📁 ios/                          # ملفات iOS
│
├── 📁 lib/                          # الكود الرئيسي
│   │
│   ├── 📁 config/                   # ملفات التكوين
│   │   └── supabase_config.dart     # إعدادات Supabase
│   │
│   ├── 📁 constants/                # الثوابت
│   │   ├── app_colors.dart          # ألوان التطبيق
│   │   ├── app_strings.dart         # النصوص الثابتة
│   │   └── app_theme.dart           # سمة التطبيق
│   │
│   ├── 📁 models/                   # نماذج البيانات
│   │   ├── admin_model.dart         # نموذج الأدمن
│   │   ├── donor_model.dart         # نموذج المتبرع
│   │   ├── hospital_model.dart      # نموذج المستشفى
│   │   ├── report_model.dart        # نموذج البلاغ
│   │   └── statistics_model.dart    # نموذج الإحصائيات
│   │
│   ├── 📁 providers/                # State Management
│   │   ├── auth_provider.dart       # إدارة المصادقة
│   │   ├── donor_provider.dart      # إدارة المتبرعين
│   │   └── statistics_provider.dart # إدارة الإحصائيات
│   │
│   ├── 📁 screens/                  # شاشات التطبيق
│   │   │
│   │   ├── 📁 admin/                # شاشات الأدمن
│   │   │   └── admin_dashboard_screen.dart
│   │   │
│   │   ├── 📁 awareness/            # التوعية
│   │   │   └── awareness_screen.dart
│   │   │
│   │   ├── 📁 donor/                # إدارة المتبرعين
│   │   │   ├── add_donor_screen.dart
│   │   │   └── search_donors_screen.dart
│   │   │
│   │   ├── 📁 home/                 # الصفحة الرئيسية
│   │   │   ├── home_screen.dart
│   │   │   └── widgets/
│   │   │       └── statistics_section.dart
│   │   │
│   │   ├── 📁 hospital/             # شاشات المستشفى
│   │   │   └── hospital_dashboard_screen.dart
│   │   │
│   │   └── 📁 reports/              # البلاغات
│   │       └── report_donor_screen.dart
│   │
│   ├── 📁 services/                 # خدمات API
│   │   ├── donor_service.dart       # خدمة المتبرعين
│   │   ├── report_service.dart      # خدمة البلاغات
│   │   ├── statistics_service.dart  # خدمة الإحصائيات
│   │   └── supabase_service.dart    # خدمة Supabase الرئيسية
│   │
│   ├── 📁 utils/                    # أدوات مساعدة
│   │   ├── helpers.dart             # دوال مساعدة
│   │   └── validators.dart          # دوال التحقق
│   │
│   ├── 📁 widgets/                  # Widgets قابلة لإعادة الاستخدام
│   │   ├── custom_dropdown.dart     # قائمة منسدلة مخصصة
│   │   ├── custom_text_field.dart   # حقل نصي مخصص
│   │   ├── donor_card.dart          # بطاقة المتبرع
│   │   ├── empty_state.dart         # حالة فارغة
│   │   └── loading_widget.dart      # ويدجت التحميل
│   │
│   └── main.dart                    # نقطة البداية
│
├── 📁 test/                         # الاختبارات
│
├── 📁 web/                          # ملفات الويب
│
├── 📄 .gitignore                    # ملفات محذوفة من Git
├── 📄 CHANGELOG.md                  # سجل التغييرات
├── 📄 CONTRIBUTING.md               # دليل المساهمة
├── 📄 DEPLOYMENT.md                 # دليل النشر
├── 📄 LICENSE                       # الترخيص
├── 📄 PROJECT_STRUCTURE.md          # هذا الملف
├── 📄 QUICK_START.md                # دليل البدء السريع
├── 📄 README.md                     # الملف التعريفي الرئيسي
├── 📄 SUPABASE_SETUP.md             # دليل إعداد Supabase
├── 📄 analysis_options.yaml         # إعدادات Linter
├── 📄 pubspec.yaml                  # تبعيات المشروع
├── 📄 sample_data.sql               # بيانات تجريبية
└── 📄 supabase_schema.sql           # Schema قاعدة البيانات
```

---

## 📋 شرح تفصيلي للمجلدات

### 1. `lib/config/` - ملفات التكوين

**الغرض**: تخزين إعدادات التطبيق الأساسية

```dart
// supabase_config.dart
// يحتوي على URL و API Keys لـ Supabase
```

**ملاحظة**: لا تضع المفاتيح الحقيقية في Git!

---

### 2. `lib/constants/` - الثوابت

**الغرض**: تخزين جميع الثوابت في مكان واحد

#### `app_colors.dart`
- ألوان التطبيق (Primary, Secondary, Success, etc.)
- ألوان فصائل الدم
- ألوان الحالات

#### `app_strings.dart`
- جميع النصوص في التطبيق
- يسهل الترجمة لاحقاً
- مركزية إدارة النصوص

#### `app_theme.dart`
- سمة Material Design 3
- أنماط الخطوط (IBM Plex Sans Arabic)
- أنماط الأزرار والبطاقات

---

### 3. `lib/models/` - نماذج البيانات

**الغرض**: تمثيل البيانات كـ Classes

كل نموذج يحتوي على:
- Properties (الخصائص)
- `fromJson()` - تحويل من JSON
- `toJson()` - تحويل إلى JSON
- `copyWith()` - نسخ مع تعديلات
- Getters مساعدة

**مثال**:
```dart
class DonorModel {
  final String id;
  final String name;
  // ...
  
  factory DonorModel.fromJson(Map<String, dynamic> json) { }
  Map<String, dynamic> toJson() { }
  DonorModel copyWith({...}) { }
  
  bool get canDonateNow { }
}
```

---

### 4. `lib/providers/` - إدارة الحالة

**الغرض**: إدارة حالة التطبيق باستخدام Provider

#### `auth_provider.dart`
- تسجيل الدخول/الخروج
- حالة المستخدم الحالي
- نوع المستخدم (Admin/Hospital)

#### `donor_provider.dart`
- البحث عن متبرعين
- إضافة/تعديل/حذف متبرعين
- إيقاف متبرعين

#### `statistics_provider.dart`
- تحميل الإحصائيات
- تحديث الإحصائيات

---

### 5. `lib/screens/` - شاشات التطبيق

**الغرض**: جميع واجهات المستخدم

منظمة حسب الوظيفة:
- `home/` - الصفحة الرئيسية والإحصائيات
- `donor/` - البحث والإضافة
- `awareness/` - التوعية والإرشادات
- `reports/` - الإبلاغ عن متبرعين
- `hospital/` - لوحة المستشفى
- `admin/` - لوحة الأدمن

كل screen يحتوي على:
- StatefulWidget أو StatelessWidget
- Build method
- منطق الـ UI
- استدعاءات للـ Providers

---

### 6. `lib/services/` - خدمات API

**الغرض**: التواصل مع Supabase

كل service يحتوي على:
- دوال للـ CRUD operations
- معالجة الأخطاء
- تحويل البيانات

**مثال**:
```dart
class DonorService {
  Future<List<DonorModel>> searchDonors(...) async { }
  Future<DonorModel> addDonor(...) async { }
  Future<DonorModel> updateDonor(...) async { }
  Future<void> deleteDonor(...) async { }
}
```

---

### 7. `lib/utils/` - أدوات مساعدة

**الغرض**: دوال utility قابلة لإعادة الاستخدام

#### `validators.dart`
- التحقق من المدخلات
- التحقق من الأسماء، الأرقام، البريد، إلخ

#### `helpers.dart`
- تنسيق التواريخ
- تنسيق أرقام الهاتف
- فتح WhatsApp
- دوال مساعدة أخرى

---

### 8. `lib/widgets/` - Widgets قابلة لإعادة الاستخدام

**الغرض**: مكونات UI مشتركة

- `donor_card.dart` - بطاقة عرض متبرع
- `custom_text_field.dart` - حقل إدخال مخصص
- `custom_dropdown.dart` - قائمة منسدلة
- `loading_widget.dart` - مؤشر التحميل
- `empty_state.dart` - حالة البيانات الفارغة

---

## 🔄 تدفق البيانات

```
┌─────────────┐
│   Screen    │
│  (UI)       │
└──────┬──────┘
       │
       │ calls
       ▼
┌─────────────┐
│  Provider   │
│  (State)    │
└──────┬──────┘
       │
       │ calls
       ▼
┌─────────────┐
│  Service    │
│  (API)      │
└──────┬──────┘
       │
       │ requests
       ▼
┌─────────────┐
│  Supabase   │
│  (Backend)  │
└─────────────┘
```

---

## 🎯 أفضل الممارسات المتبعة

### 1. **Separation of Concerns**
- UI منفصلة عن المنطق
- Services منفصلة عن UI
- Models منفصلة عن كل شيء

### 2. **Single Responsibility**
- كل class/function لها مسؤولية واحدة
- سهولة الصيانة والاختبار

### 3. **DRY (Don't Repeat Yourself)**
- Widgets قابلة لإعادة الاستخدام
- Utilities مركزية
- Constants في مكان واحد

### 4. **Clean Code**
- أسماء واضحة ومعبرة
- تعليقات باللغة العربية
- كود منظم ومرتب

---

## 📚 إضافة ميزة جديدة

### مثال: إضافة ميزة "المفضلة"

1. **Model**: أضف في `donor_model.dart`
```dart
final bool isFavorite;
```

2. **Service**: أضف في `donor_service.dart`
```dart
Future<void> toggleFavorite(String id) async { }
```

3. **Provider**: أضف في `donor_provider.dart`
```dart
Future<void> toggleFavorite(String id) async { }
```

4. **UI**: أضف في `donor_card.dart`
```dart
IconButton(
  icon: Icon(donor.isFavorite ? Icons.favorite : Icons.favorite_border),
  onPressed: () => provider.toggleFavorite(donor.id),
)
```

5. **Database**: أضف عمود في Supabase
```sql
ALTER TABLE donors ADD COLUMN is_favorite BOOLEAN DEFAULT FALSE;
```

---

## 🧪 الاختبارات (قريباً)

```
test/
├── models/
│   └── donor_model_test.dart
├── services/
│   └── donor_service_test.dart
├── providers/
│   └── donor_provider_test.dart
└── widgets/
    └── donor_card_test.dart
```

---

## 📖 مصادر إضافية

- [Flutter Documentation](https://docs.flutter.dev/)
- [Supabase Documentation](https://supabase.com/docs)
- [Provider Package](https://pub.dev/packages/provider)
- [Material Design 3](https://m3.material.io/)

---

**هذا الهيكل قابل للتوسع والتطوير مستقبلاً** 🚀

