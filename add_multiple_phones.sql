-- ============================================
-- اضافة امكانية ارقام هواتف متعددة للمتبرع
-- ============================================

-- اضافة عمودين جديدين للارقام الاضافية
ALTER TABLE public.donors 
ADD COLUMN IF NOT EXISTS phone_number_2 VARCHAR(20),
ADD COLUMN IF NOT EXISTS phone_number_3 VARCHAR(20);

-- اضافة فهرس للارقام الاضافية
CREATE INDEX IF NOT EXISTS idx_donors_phone_number_2 ON donors(phone_number_2);
CREATE INDEX IF NOT EXISTS idx_donors_phone_number_3 ON donors(phone_number_3);

-- عرض النتيجة
SELECT 
    '✅ تم اضافة الاعمدة الجديدة' as status;

SELECT 
    column_name,
    data_type
FROM information_schema.columns
WHERE table_name = 'donors' 
  AND column_name LIKE 'phone%'
ORDER BY column_name;

-- ============================================
-- ملاحظات
-- ============================================

/*
✅ الآن كل متبرع يمكنه امتلاك:
   - phone_number (الرئيسي - اجباري)
   - phone_number_2 (اضافي - اختياري)
   - phone_number_3 (اضافي - اختياري)

🎯 الاستخدام:
   - في شاشة اضافة/تعديل متبرع
   - سيظهر 3 حقول للارقام
   - الاول اجباري، والآخران اختياري

📝 البحث:
   - سيتم تحديث دالة البحث للبحث في الارقام الثلاثة
*/

