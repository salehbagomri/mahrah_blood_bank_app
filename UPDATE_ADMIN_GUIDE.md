# 🔐 دليل تحديث بيانات الأدمن

## 📋 المعلومات

### البيانات القديمة ❌:
```
Email: s.bagomri@gmail.com
Password: s770727055
```

### البيانات الجديدة ✅:
```
Email: admin@test.com
Password: test123456
```

---

## 🚀 خطوات التنفيذ

### الطريقة 1: استخدام السكربت SQL (موصى بها) ⭐

#### 1. افتح Supabase Dashboard:
```
https://supabase.com/dashboard
```

#### 2. اذهب إلى SQL Editor:
```
Dashboard → Your Project → SQL Editor
```

#### 3. انسخ والصق السكربت:
- افتح ملف `update_admin_credentials.sql`
- انسخ المحتوى بالكامل
- الصقه في SQL Editor

#### 4. نفّذ السكربت:
```
اضغط على زر "Run" أو Ctrl+Enter
```

#### 5. تحقق من النتائج:
```
يجب أن ترى رسائل:
✅ تم العثور على المستخدم
✅ تم تحديث الإيميل في auth.users
✅ تم تحديث الإيميل في public.admins
✅ تم تحديث كلمة المرور
✅ تم التحديث بنجاح!
```

---

### الطريقة 2: تحديث يدوي من Dashboard (بديلة)

#### تحديث الإيميل:

1. اذهب إلى Authentication → Users
2. ابحث عن المستخدم: `s.bagomri@gmail.com`
3. اضغط على السطر
4. اختر "Edit User"
5. غيّر Email إلى: `admin@test.com`
6. احفظ التغييرات

#### تحديث كلمة المرور:

##### الخيار A - Reset Password:
1. في صفحة المستخدم، اضغط "Send recovery email"
2. افتح البريد الإلكتروني (admin@test.com)
3. اضغط على الرابط
4. أدخل كلمة المرور الجديدة: `test123456`

##### الخيار B - Direct Update:
1. في صفحة Edit User
2. أدخل كلمة المرور الجديدة: `test123456`
3. احفظ

---

## 🔍 التحقق من التحديث

### من SQL Editor:
```sql
-- عرض بيانات الأدمن
SELECT 
  u.id,
  u.email,
  u.created_at,
  a.name AS admin_name,
  a.is_active
FROM auth.users u
JOIN public.admins a ON a.id = u.id
WHERE u.email = 'admin@test.com';
```

### من التطبيق:
1. افتح التطبيق
2. اذهب إلى شاشة تسجيل الدخول
3. أدخل:
   - Email: `admin@test.com`
   - Password: `test123456`
4. اضغط "تسجيل الدخول"
5. يجب أن تدخل بنجاح! ✅

---

## ⚠️ ملاحظات مهمة

### الأمان:
```
⚠️ كلمة المرور test123456 للاختبار فقط
⚠️ غيّرها إلى كلمة مرور قوية بعد أول تسجيل دخول
⚠️ استخدم: أحرف كبيرة + صغيرة + أرقام + رموز
```

### الصلاحيات:
```
✅ يجب أن يكون لديك Owner أو Admin في Supabase
✅ لا تشارك بيانات الدخول مع أحد
```

### النسخ الاحتياطي:
```
💡 قبل التنفيذ، احفظ البيانات القديمة:
   Email: s.bagomri@gmail.com
   Password: s770727055
   
   في حالة احتجت الرجوع إليها
```

---

## 🛠️ استكشاف الأخطاء

### خطأ: "لم يتم العثور على المستخدم"
```sql
-- تحقق من الإيميل الحالي
SELECT email FROM auth.users 
WHERE email LIKE '%bagomri%';
```

**الحل:** تأكد من الإيميل الحالي صحيح

---

### خطأ: "permission denied"
```
الحل: 
1. تأكد أنك Owner في المشروع
2. أو استخدم Service Role Key
```

---

### خطأ: "function crypt does not exist"
```sql
-- قم بتفعيل extension pgcrypto أولاً
CREATE EXTENSION IF NOT EXISTS pgcrypto;

-- ثم أعد تنفيذ السكربت
```

---

### لا يمكن تسجيل الدخول بعد التحديث
```
1. امسح cache التطبيق
2. تأكد من الإيميل صحيح: admin@test.com
3. تأكد من كلمة المرور: test123456
4. تحقق من Supabase Dashboard أن المستخدم نشط
```

---

## 📊 السكربت يقوم بـ:

```
1. ✅ البحث عن المستخدم بالإيميل القديم
2. ✅ تحديث الإيميل في auth.users
3. ✅ تحديث الإيميل في public.admins
4. ✅ تحديث كلمة المرور (مشفرة)
5. ✅ عرض ملخص التحديثات
6. ✅ عرض البيانات الجديدة للتحقق
```

---

## 🎯 بعد التحديث

### غيّر كلمة المرور:
1. سجل دخول بـ `admin@test.com` / `test123456`
2. اذهب إلى الإعدادات
3. غيّر كلمة المرور إلى كلمة قوية
4. مثال كلمة مرور قوية:
   ```
   Admin@Mahrah2024!#
   ```

### تحديث في `public.admins`:
```sql
-- تحديث اسم الأدمن إذا أردت
UPDATE public.admins
SET 
  name = 'المدير العام',
  updated_at = NOW()
WHERE email = 'admin@test.com';
```

---

## ✅ checklist

```
□ نسخ السكربت من update_admin_credentials.sql
□ فتح Supabase SQL Editor
□ لصق السكربت
□ تنفيذ السكربت (Run)
□ التحقق من رسائل النجاح
□ اختبار تسجيل الدخول من التطبيق
□ تغيير كلمة المرور إلى كلمة قوية
□ حذف السكربت من SQL Editor (للأمان)
```

---

## 📞 في حالة المشاكل

### إذا لم يعمل السكربت:
```
استخدم الطريقة اليدوية (الطريقة 2)
أو تواصل مع دعم Supabase
```

### إذا نسيت كلمة المرور الجديدة:
```
استخدم "Forgot Password" من التطبيق
أو أعد تنفيذ السكربت بكلمة مرور جديدة
```

---

**🎊 بالتوفيق!** 🚀

